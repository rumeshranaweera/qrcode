import type { ThemeConfig } from "antd";

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
    colorPrimary: "#fda530",
    padding: 100,
    colorText: "#52c41a",
  },
};

export default theme;
