import { createContext } from "react";

const useInput = createContext(null);

const ContextAPI = () => {
  //   return <useInput.Provider value={}>ContextAPI</useInput.Provider>;
};

export default ContextAPI;
